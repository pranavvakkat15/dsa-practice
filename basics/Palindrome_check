class Solution:
    def isPalindrome(self, n: int) -> bool:
        original = n
        reverseNumber = 0
        while n > 0:
            ld = n % 10
            reverseNumber = (reverseNumber * 10) + ld
            n = n // 10
        return reverseNumber == original
